import { Router } from 'express'; import * as c from '../controllers/project.controller.js'; import { authenticate } from '../middlewares/auth.middleware.js';
export const projectRouter = Router(); projectRouter.use(authenticate); projectRouter.get('/', c.listProjects); projectRouter.post('/', c.createProject); projectRouter.get('/:projectId', c.getProject); projectRouter.patch('/:projectId', c.updateProject); projectRouter.delete('/:projectId', c.deleteProject); projectRouter.post('/:projectId/members', c.inviteMember); projectRouter.delete('/:projectId/members/:userId', c.removeMember);
