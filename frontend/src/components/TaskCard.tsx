import { useSortable } from '@dnd-kit/sortable'; import { CSS } from '@dnd-kit/utilities'; import { CalendarDays, MessageSquare } from 'lucide-react'; import { PriorityBadge } from './Badge'; import type { Task } from '../types';
export function TaskCard({ task, onClick }: { task: Task; onClick: () => void }) { const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: task.id }); const overdue = task.dueDate && new Date(task.dueDate) < new Date() && task.status !== 'DONE'; return <article ref={setNodeRef} style={{ transform: CSS.Transform.toString(transform), transition }} {...attributes} {...listeners} onClick={onClick} className={`cursor-grab rounded-lg border border-slate-200 bg-white p-3 shadow-sm active:cursor-grabbing ${isDragging ? 'opacity-40' : ''}`}><div className="flex justify-between gap-2"><h4 className="line-clamp-2 text-sm font-medium text-ink">{task.title}</h4><PriorityBadge priority={task.priority}/></div>{task.description && <p className="mt-2 line-clamp-2 text-xs text-slate-500">{task.description}</p>}<div className="mt-3 flex items-center justify-between text-xs text-slate-500"><span className={overdue ? 'text-rose-600' : ''}>{task.dueDate && <><CalendarDays className="mr-1 inline" size={13}/>{new Date(task.dueDate).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })}</>}</span><div className="flex items-center gap-2">{(task.comments?.length ?? 0) > 0 && <span><MessageSquare className="mr-0.5 inline" size={13}/>{task.comments?.length}</span>}{task.assignedUser && <span title={task.assignedUser.name} className="flex h-5 w-5 items-center justify-center rounded-full bg-indigo-100 text-[10px] font-semibold text-indigo-700">{task.assignedUser.name.slice(0, 1).toUpperCase()}</span>}</div></div></article>; }
